# .Rprofile — Project-local R configuration

# Activate renv if available
if (file.exists("renv/activate.R")) {
  # source("renv/activate.R")
}

# Set targets project root
Sys.setenv(TAR_PROJECT = getwd())

# Optional: quiet startup
options(digits = 4, scipen = 999)
